<?xml version="1.0" encoding="utf-8"?>
<!-- Diagnostic build: the floating bubble inspects WhatsApp's accessibility tree. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <service
            android:name=".BubbleService"
            android:exported="true"
            android:label="ZivugBase bubble"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/bubble_service" />
        </service>

        <activity
            android:name=".DiagnosticActivity"
            android:exported="false"
            android:label="WhatsApp Accessibility Test" />
    </application>
</manifest>
